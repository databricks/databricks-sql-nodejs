*

!dist/**/*
!thrift/**/*

# SEA napi-rs router shim + TypeScript declarations. The router (index.js)
# selects the per-platform `.node` artifact from `@databricks/sql-kernel-*`
# optionalDependencies (populated when the kernel CI publishes them);
# the .d.ts is the consumer-facing type contract.
!native/sea/index.js
!native/sea/index.d.ts
# [bench-only] ship the prebuilt linux-x64-gnu binary in the package so the
# github-resolved driver is self-contained for the hermetic DUST benchmark.
# NOT for merge — production ships it via the kernel per-triple optional deps.
!native/sea/index.linux-x64-gnu.node

!LICENSE
!NOTICE
!package.json
!README.md
